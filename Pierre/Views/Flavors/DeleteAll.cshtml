@{
  Layout = "_Layout";
}
@model List<Pierre.Models.Flavor>;
  
<div class="container card">
    <h1>Delete all Flavors?</h1>
    <hr>
    @using (Html.BeginForm())
    {
        <button class="btn btn-warning" type="submit" value="DeleteAll">YES</button>
    }
    <hr>
    <br>
    <input class="btn btn-warning" type="button" value="Flavors" onclick='location.href="@Url.Action("Index", "Flavor")";' />
    <input class="btn btn-warning" type="button" value="Home" onclick='location.href="@Url.Action("Index", "Home")";' />
</div>